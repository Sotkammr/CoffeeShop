<Page x:Class="CoffeeShop.Pages.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Управление товарами">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" 
                   Text="📦 УПРАВЛЕНИЕ ТОВАРАМИ" 
                   FontSize="32" 
                   FontWeight="Bold" 
                   Foreground="#795548"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,20">
            <Button x:Name="btnAddProduct" 
                    Content="➕ Добавить товар" 
                    Height="40" 
                    Width="150"
                    Background="#4CAF50"
                    Foreground="White"
                    FontWeight="Bold"
                    Margin="0,0,10,0"
                    Click="btnAddProduct_Click"/>
            <Button x:Name="btnEditProduct" 
                    Content="✏️ Изменить" 
                    Height="40" 
                    Width="120"
                    Background="#2196F3"
                    Foreground="White"
                    FontWeight="Bold"
                    Margin="0,0,10,0"
                    IsEnabled="False"
                    Click="btnEditProduct_Click"/>
            <Button x:Name="btnDeleteProduct" 
                    Content="🗑️ Удалить" 
                    Height="40" 
                    Width="120"
                    Background="#F44336"
                    Foreground="White"
                    FontWeight="Bold"
                    Margin="0,0,10,0"
                    IsEnabled="False"
                    Click="btnDeleteProduct_Click"/>

            <Button x:Name="btnRefresh" 
                    Content="🔄 Обновить" 
                    Height="40" 
                    Width="120"
                    Background="#FF9800"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="btnRefresh_Click"/>
        </StackPanel>
        <DataGrid Grid.Row="2" 
                  x:Name="dgProducts" 
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionChanged="dgProducts_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="150"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding PriceDisplay}" Width="120"/>
                <DataGridTextColumn Header="В наличии" Binding="{Binding QuantityInStock}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
